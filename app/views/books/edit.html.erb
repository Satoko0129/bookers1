<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>WebCampBackendNew</title>
  </head>

  <body>
    <h1>Editing Book</h1>
    <% if @book.errors.any? %>
      <%= @book.errors.count %>errors prohibited this book from being saved:
        <ul>
          <% @book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
      <% end %>
        </ul>
    <% end %>

    <div class="edit-text">
      <%= form_with model: @book ,url: book_path(@book.id) do |f| %>
        <div class="field">
          <label for="book_title">Title</label>
          <%= f.text_field :title %>
        </div>

        <div class="field">
          <label for="book_body">Body</label>
          <%= f.text_area :body %>
        </div>

        <div class="actions">
          <%= f.submit 'Update Book' %>
        </div>
      <% end %>
    </div>

    <div class="show-link"></div>
      <%= link_to "Show", book_path(@book) %><span>|</span><%= link_to "Back", books_path %></a>
  </body>
</html>